% =============================================================================
% =============================================================================
% CAPÍTULO 4 --- COMPONENTE 2: BIOREMPP DATABASE EXPLORER (DBX)
% =============================================================================
% =============================================================================
\chapter{Componente 2: BioRemPP Database Explorer (DBX)}
\label{cap:comp2}

% -----------------------------------------------------------------------------
\section{Motivação e escopo do componente}
\label{sec:c2-motivacao}
% -----------------------------------------------------------------------------

O banco integrado descrito no Capítulo~\ref{cap:comp1} é distribuído em
arquivos CSV e XLSX associados a uma versão publicada. Esse formato preserva
o conteúdo científico, permite sua reutilização programática e mantém o
registro das entidades e relações incorporadas ao banco. Entretanto, a
distribuição por arquivos não oferece, isoladamente, uma camada de consulta
interativa.

A exploração direta desses arquivos exige conhecimento prévio do esquema de
dados e das relações entre compostos, KOs, números EC, reações, vias,
referências institucionais e predições toxicológicas. A estrutura
denormalizada também exige que o pesquisador diferencie o número de linhas das
cardinalidades de cada entidade e reconstrua localmente associações
muitos-para-muitos. Essas operações podem ser executadas em R, Python ou
sistemas de banco de dados, mas dependem da escrita de código e da preparação
do ambiente de análise.

O \gls{dbx} fornece uma camada de acesso ao conteúdo integrado sem substituir
os arquivos científicos publicados. A aplicação organiza a evidência em uma
estrutura orientada à consulta e a disponibiliza por uma interface web. O
usuário pode pesquisar entidades, aplicar filtros e percorrer relações entre
compostos, grupos de ortologia KEGG, vias metabólicas, classes químicas,
referências institucionais e predições toxicológicas sem reconstruir
localmente as associações do banco.

A navegação é centrada no composto porque as informações recuperadas pela
interface convergem para as substâncias incluídas no universo institucional do
BioRemPP. Essa organização não reduz todas as evidências a um único
identificador. Os identificadores \gls{cpd} preservam a referência química e
a ligação com as predições toxicológicas, enquanto os identificadores
\gls{ko} conectam os compostos às anotações funcionais do HADEG e do
subconjunto KEGG Degradation. CPD e KO permanecem, portanto, como entidades
complementares de consulta.

A aplicação disponibiliza módulos dedicados a compostos, classes químicas,
genes e KOs, vias e toxicidade. Esses módulos permitem consultar registros
individuais e percorrer entidades relacionadas. O componente também reúne
oito análises guiadas que formalizam perguntas sobre amplitude de anotação
funcional, toxicidade predita, classificação institucional e conectividade
entre compostos, KOs e vias. Os filtros e parâmetros dessas análises tornam
explícito o recorte aplicado a cada resultado.

O \gls{dbx} não recebe conjuntos de dados do usuário para processamento. Sua
função é consultar o acervo integrado e versionado. Essa característica o
distingue do \gls{bws}, apresentado no Capítulo~\ref{cap:comp3}, que processa
anotações funcionais submetidas pelo usuário para produzir perfis associados
às amostras analisadas. O Database Explorer opera sobre um conjunto de
referência previamente construído, enquanto o Web Service aplica as relações
do BioRemPP a dados externos.

O escopo do \gls{dbx} é exploratório. As associações exibidas representam
registros curados, relações recuperadas de bases públicas e predições
computacionais. Elas sustentam a formulação de hipóteses e a priorização de
entidades para análises posteriores, mas não confirmam a degradação de um
composto nem sua toxicidade em condições ambientais.

A presença de uma substância em uma fonte institucional também não constitui
avaliação jurídica ou determinação de risco para um local específico. Da
mesma forma, a ausência de uma relação na interface pode refletir os limites
de cobertura ou a versão das fontes integradas. A interpretação dos resultados
deve considerar a proveniência, a natureza da evidência e os critérios usados
na construção do banco.

% -----------------------------------------------------------------------------
\section{Arquitetura e organização dos dados}
\label{sec:c2-arquitetura}
% -----------------------------------------------------------------------------

O banco integrado é publicado em arquivos CSV e XLSX com estrutura
denormalizada, adequada à distribuição, à preservação e ao processamento
programático. Essa organização mantém no mesmo arquivo as combinações entre
compostos, KOs, números EC, reações, classes químicas e referências
institucionais. Seu uso direto pela aplicação exigiria ler, filtrar e agrupar
essas combinações a cada requisição.

O BioRemPP Database Explorer usa um banco SQLite derivado dos conjuntos
publicados. Essa representação mantém apenas as estruturas necessárias às
consultas da aplicação e permite selecionar, ordenar, agrupar e paginar os
registros sem processar novamente a tabela denormalizada. O formato em arquivo
único também reduz os componentes necessários para disponibilizar o banco no
ambiente de execução.

A camada interna de API orquestra o acesso entre a interface web e o SQLite. A
interface envia os parâmetros de busca, filtragem e paginação, enquanto a API
executa as consultas correspondentes e retorna os resultados em estruturas
consumidas pelos módulos da aplicação. O frontend não acessa diretamente o
banco nem processa os arquivos CSV e XLSX.

Antes da inicialização, o servidor verifica a presença do arquivo SQLite e das
tabelas exigidas pela aplicação. O banco é aberto em modo somente leitura, o
que impede alterações durante as consultas. A API organiza o acesso aos dados
por domínio, com operações específicas para compostos, genes e KOs, vias,
toxicidade, metadados e análises guiadas.

\subsection{Construção do banco SQLite}

A construção do SQLite usa como entradas a tabela central BioRemPP, o HADEG,
o subconjunto KEGG Degradation e o ToxCSM. As informações do HADEG e do
subconjunto KEGG Degradation são associadas à tabela central por KO, enquanto
as predições toxicológicas são relacionadas por CPD.

O processo verifica os identificadores necessários às associações e agrupa os
valores distintos de cada entidade. Os registros são reorganizados por
composto, KO, via, referência institucional e endpoint toxicológico. Também
são preservadas as combinações entre composto, KO, número EC, reação, via e
fonte necessárias às páginas de detalhe e às análises guiadas.

O SQLite é reconstruído a partir dos conjuntos correspondentes a cada versão
do banco. Após a carga, verificações de consistência comparam as contagens
armazenadas nos resumos por entidade com os registros que lhes dão origem. Os
resultados quantitativos da construção e dessas verificações são apresentados
na Seção~\ref{sec:c2-resultados}.

A Figura~\ref{fig:c2-data-architecture} resume o fluxo entre os conjuntos de
entrada, o banco SQLite, a API interna e a interface web.

\begin{figure}[htbp]
\centering
\fbox{
    \parbox{0.92\textwidth}{
        \centering

        \textbf{Conjuntos de entrada}\\[0.4em]
        BioRemPP central
        \quad HADEG
        \quad KEGG Degradation
        \quad ToxCSM

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{Associação por KO e CPD}\\
        verificação dos identificadores e agrupamento dos registros

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{Banco SQLite}\\
        resumos por entidade, mapeamentos e relações contextuais

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{API interna}\\
        busca, filtragem, ordenação, paginação e análises guiadas

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{Interface web}\\
        exploradores, páginas de detalhe e visualizações

        \vspace{0.8em}
    }
}
\caption{Arquitetura de dados do BioRemPP Database Explorer. Os conjuntos de
entrada são associados por KO ou CPD e reorganizados em um banco SQLite. A
API interna consulta esse banco e fornece os resultados apresentados pela
interface web.}
\label{fig:c2-data-architecture}
\end{figure}

Os arquivos publicados e o SQLite atendem a finalidades distintas. Os
arquivos CSV e XLSX preservam e distribuem o conteúdo integrado, enquanto o
SQLite fornece uma representação compacta para as operações da aplicação.
Como o banco de consulta pode ser reconstruído a partir das entradas
versionadas, ele não substitui o artefato científico depositado.

\subsection{Organização dos dados e consulta pela API}

O banco SQLite contém estruturas voltadas a quatro tipos de operação:
consultas por entidade, recuperação de associações, reconstrução do contexto
funcional e acesso às predições toxicológicas. Essa organização evita
recalcular, durante cada consulta, as agregações presentes na tabela
denormalizada.

A Tabela~\ref{tab:c2-data-groups} resume a função desses grupos. Os nomes
completos das tabelas, os campos, os tipos e os índices permanecem registrados
na documentação oficial do projeto.

\begin{table}[htbp]
\centering
\caption{Grupos de dados usados pelo BioRemPP Database Explorer.}
\label{tab:c2-data-groups}
\small
\begin{tabularx}{\textwidth}{@{}p{3.8cm}X@{}}
\toprule
\textbf{Grupo} & \textbf{Função na aplicação} \\
\midrule

Resumos por entidade
& Mantêm uma representação por composto, KO ou via e as contagens usadas em
buscas, filtros e ordenações. \\

Mapeamentos entre entidades
& Registram as associações entre compostos, símbolos gênicos, vias e
referências institucionais. \\

Contexto funcional
& Preserva as combinações entre CPD, KO, atividade enzimática, número EC,
reação, via e fonte. \\

Predições e metadados
& Armazena os valores por combinação composto--endpoint e as informações de
identificação e proveniência associadas ao CPD. \\

\bottomrule
\end{tabularx}
\end{table}

Os resumos por entidade fornecem os campos usados nas listagens e nos filtros
da interface. Para os compostos, esses campos incluem nome, classe química e
contagens de KOs, vias e referências institucionais. A classe química funciona
como atributo de exibição, agrupamento e filtragem; ela não participa das
associações funcionais estabelecidas por KO ou CPD.

As associações entre compostos, KOs, vias e referências institucionais são
mantidas em estruturas próprias. O contexto das associações funcionais
preserva as combinações necessárias para recuperar números EC, reações,
atividades enzimáticas e vias relacionadas a cada composto. As predições
toxicológicas permanecem organizadas por composto e endpoint.

A Figura~\ref{fig:c2-relational-model} apresenta uma visão conceitual dessas
associações. Campos auxiliares e estruturas exclusivas da implementação foram
omitidos.

\begin{figure}[htbp]
\centering
\fbox{
    \parbox{0.88\textwidth}{
        \centering

        \textbf{KOs e anotações funcionais}

        \vspace{0.5em}
        $\updownarrow$

        \vspace{0.5em}
        \textbf{Composto}\\
        CPD, nome, classe química e contagens associadas

        \vspace{0.5em}

        $\swarrow$
        \hspace{4em}
        $\downarrow$
        \hspace{4em}
        $\searrow$

        \vspace{0.5em}

        \textbf{Vias}
        \hspace{3em}
        \textbf{Referências}
        \hspace{3em}
        \textbf{Toxicidade}

        \vspace{0.5em}

        associações por KO
        \hspace{2em}
        fontes institucionais
        \hspace{2em}
        endpoints por CPD

        \vspace{0.8em}
    }
}
\caption{Organização conceitual das consultas no banco SQLite. O composto
constitui o principal ponto de navegação, com associações a KOs, vias,
referências institucionais e endpoints toxicológicos.}
\label{fig:c2-relational-model}
\end{figure}

Durante a execução, a API consulta exclusivamente o SQLite em modo somente
leitura. As operações são organizadas segundo os módulos da aplicação e
retornam apenas os registros necessários à página solicitada. Os arquivos de
origem não são carregados nem transformados durante essas requisições.

Os arquivos CSV e XLSX preservam o banco integrado para distribuição e
reutilização. O SQLite sustenta as consultas da aplicação, e a API controla o
acesso a essa representação. Cada formato permanece associado à função para a
qual foi produzido.

% -----------------------------------------------------------------------------
\section{Exploração e análises guiadas}
\label{sec:c2-exploracao}
% -----------------------------------------------------------------------------

O BioRemPP Database Explorer oferece duas formas de consulta ao banco
integrado. Os módulos de exploração permitem definir livremente o ponto de
entrada, os filtros e a ordenação dos registros. As análises guiadas partem de
perguntas previamente estruturadas e aplicam operações analíticas definidas
para compostos, vias, genes e KOs.

As duas formas de consulta usam a mesma representação SQLite e preservam a
navegação entre entidades relacionadas. Um registro recuperado em uma
listagem pode ser examinado em uma página de detalhe, na qual são reunidas as
associações funcionais, institucionais ou toxicológicas disponíveis para a
entidade selecionada.

\subsection{Módulos de exploração}

A interface organiza o banco em cinco módulos definidos pela unidade de
consulta: compostos, classes químicas, genes e KOs, vias e toxicidade. Os
módulos compartilham uma sequência de interação baseada em busca, filtragem,
ordenação e inspeção do registro. A entidade representada em cada linha e os
campos usados nos filtros variam conforme o domínio consultado.

A Tabela~\ref{tab:c2-explorer-modules} resume a unidade de consulta, os
principais recortes e as relações recuperadas por cada módulo. A descrição
completa dos campos exibidos permanece na documentação oficial da aplicação.

\begin{table}[htbp]
\centering
\caption{Módulos de exploração do BioRemPP Database Explorer.}
\label{tab:c2-explorer-modules}
\footnotesize
\begin{tabularx}{\textwidth}{
    @{}
    p{2.4cm}
    p{2.8cm}
    p{4.7cm}
    X
    @{}
}
\toprule
\textbf{Módulo}
& \textbf{Unidade de consulta}
& \textbf{Recortes disponíveis}
& \textbf{Relações recuperadas} \\
\midrule

Compostos
& Composto identificado por CPD
& Nome ou CPD, classe química, via, símbolo gênico, referência institucional
e intervalos de contagens.
& KOs, símbolos gênicos, vias, números EC, reações, referências
institucionais e predições toxicológicas. \\

Classes químicas
& Rótulo de classe associado aos compostos
& Busca pelo nome da classe e seleção dos registros correspondentes.
& Compostos incluídos na classe e respectivas medidas de anotação. \\

Genes e KOs
& KO, com símbolo e nome gênico associados
& Busca por KO, símbolo ou nome gênico e intervalo do número de compostos.
& Compostos, vias, atividades enzimáticas e informações funcionais
associadas. \\

Vias
& Combinação entre via e fonte
& Fonte da anotação, nome da via e intervalos de contagens.
& Compostos, KOs e símbolos gênicos vinculados à via selecionada. \\

Toxicidade
& Combinação entre composto e endpoint
& Grupo de endpoints, endpoint, categoria predita, classe química e intervalo
do valor numérico.
& Perfil toxicológico predito e ligação com o registro do composto. \\

\bottomrule
\end{tabularx}
\end{table}

O módulo de compostos constitui o ponto de entrada para consultas iniciadas
por uma substância específica. Cada registro corresponde a um CPD e reúne o
nome, a classe química e contagens derivadas das associações disponíveis. Os
filtros permitem restringir o conjunto por anotações funcionais, vias,
referências institucionais e atributos usados na apresentação dos compostos.

A página de detalhe do composto recupera os KOs, símbolos gênicos, números EC,
reações e vias associados ao registro. O mesmo contexto inclui as referências
institucionais e as predições toxicológicas disponíveis, sem reduzir essas
informações a uma única medida de atividade ou risco.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]
{figures/chapter_04/fig_04_compounds_explorer.png}
\iffalse
\fbox{
    \parbox{0.90\textwidth}{
        \centering
        Inserir captura do módulo \textit{Compounds}.\\[0.5em]
        A imagem deve mostrar a busca, os filtros, a tabela de compostos e
        o acesso a um registro individual.
        \vspace{3em}
    }
}
% Substituir o placeholder por:
% \includegraphics[width=\textwidth]
% {figures/chapter_04/fig_04_compounds_explorer.png}
\fi
\caption{Módulo de exploração de compostos. A interface permite restringir
os registros por atributos químicos, anotações funcionais, vias e referências
institucionais antes da consulta ao detalhe de cada CPD.}
\label{fig:c2-compounds-explorer}
\end{figure}

O módulo de classes químicas agrupa os compostos pelo rótulo de classe usado
na interface. Essa consulta permite examinar a distribuição dos registros
entre categorias químicas e recuperar os compostos associados a cada grupo.
A classe funciona como atributo de apresentação e filtragem e não altera as
relações estabelecidas por KO ou CPD.

A seleção de uma classe conduz ao conjunto de compostos correspondente. A
consulta pode, então, prosseguir nas páginas individuais para recuperar as
anotações disponíveis para cada substância.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]
{figures/chapter_04/fig_04_compound_classes_explorer.png}
\iffalse
\fbox{
    \parbox{0.90\textwidth}{
        \centering
        Inserir captura do módulo \textit{Compound Classes}.\\[0.5em]
        A imagem deve mostrar a listagem das classes e os compostos
        recuperados para uma classe selecionada.
        \vspace{3em}
    }
}
% Substituir o placeholder por:
% \includegraphics[width=\textwidth]
% {figures/chapter_04/fig_04_compound_classes_explorer.png}
\fi
\caption{Módulo de exploração de classes químicas. Os registros são
agrupados pelo rótulo de classe usado na apresentação dos compostos e
permanecem vinculados às respectivas páginas de detalhe.}
\label{fig:c2-compound-classes-explorer}
\end{figure}

O módulo de genes e KOs inverte a direção da consulta funcional. O KO
constitui o identificador principal, enquanto o símbolo e o nome gênico
fornecem descrições associadas. A listagem informa quantos compostos e vias
estão relacionados a cada registro.

A página de detalhe recupera os compostos vinculados ao KO e o contexto
enzimático disponível, incluindo atividades, números EC e vias. As contagens
descrevem conectividade no banco integrado; elas não indicam expressão gênica,
atividade enzimática ou importância metabólica.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]
{figures/chapter_04/fig_04_genes_ko_explorer.png}
\iffalse
\fbox{
    \parbox{0.90\textwidth}{
        \centering
        Inserir captura do módulo \textit{Genes / KO}.\\[0.5em]
        A imagem deve mostrar a busca por KO ou símbolo gênico, as contagens
        associadas e a navegação para a página de detalhe.
        \vspace{3em}
    }
}
% Substituir o placeholder por:
% \includegraphics[width=\textwidth]
% {figures/chapter_04/fig_04_genes_ko_explorer.png}
\fi
\caption{Módulo de exploração de genes e KOs. A consulta recupera compostos,
vias e descritores funcionais associados a cada grupo de ortologia KEGG.}
\label{fig:c2-genes-ko-explorer}
\end{figure}

O módulo de vias mantém a fonte como parte da identificação do registro. Essa
distinção preserva as convenções de anotação do HADEG, do KEGG Degradation e
das categorias de vias de compostos sem pressupor equivalência entre seus
critérios de construção.

Cada via pode ser examinada pelo número de compostos e anotações funcionais
associados. A página de detalhe recupera os compostos e KOs que sustentam a
relação com a via selecionada. Essas contagens não correspondem à completude
da via, ao fluxo metabólico ou à atividade dos seus componentes.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]
{figures/chapter_04/fig_04_pathways_explorer.png}
\iffalse
\fbox{
    \parbox{0.90\textwidth}{
        \centering
        Inserir captura do módulo \textit{Pathways}.\\[0.5em]
        A imagem deve mostrar o filtro por fonte, a listagem de vias e o
        conjunto de compostos ou KOs exibido no detalhe.
        \vspace{3em}
    }
}
% Substituir o placeholder por:
% \includegraphics[width=\textwidth]
% {figures/chapter_04/fig_04_pathways_explorer.png}
\fi
\caption{Módulo de exploração de vias. A fonte permanece associada ao nome
da via, e o detalhe recupera os compostos e KOs relacionados ao registro.}
\label{fig:c2-pathways-explorer}
\end{figure}

O módulo de toxicidade adota a combinação composto--endpoint como unidade de
consulta. Os endpoints podem ser restringidos por grupo, categoria predita e
intervalo numérico. A seleção do endpoint precede a interpretação do valor,
pois os descritores representam propriedades toxicológicas distintas.

Os resultados mantêm o vínculo com o CPD e permitem acessar o contexto
funcional e institucional do composto correspondente. Os valores exibidos
provêm de modelos preditivos e não equivalem a medidas experimentais de
toxicidade.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]
{figures/chapter_04/fig_04_toxicity_explorer.png}
\iffalse
\fbox{
    \parbox{0.90\textwidth}{
        \centering
        Inserir captura do módulo \textit{Toxicity}.\\[0.5em]
        A imagem deve mostrar a seleção do grupo e do endpoint, os filtros
        por categoria ou valor e os compostos recuperados.
        \vspace{3em}
    }
}
% Substituir o placeholder por:
% \includegraphics[width=\textwidth]
% {figures/chapter_04/fig_04_toxicity_explorer.png}
\fi
\caption{Módulo de exploração de toxicidade. Os registros são consultados
por composto e endpoint, preservando o contexto específico de cada predição.}
\label{fig:c2-toxicity-explorer}
\end{figure}

A navegação entre listagens e páginas de detalhe conecta os cinco módulos. Um
composto recuperado por classe química ou endpoint toxicológico pode ser
examinado pelas associações com KOs e vias. De modo recíproco, um KO ou uma
via pode conduzir ao conjunto de compostos relacionado ao registro. Essa
navegação usa as relações materializadas no banco SQLite e mantém a unidade
de observação de cada módulo.

\subsection{Análises guiadas}

Os módulos de exploração permitem compor consultas por combinações livres de
filtros. As análises guiadas formalizam oito perguntas recorrentes sobre o
banco e definem previamente a unidade de consulta, a métrica, a ordenação e a
forma de apresentação dos resultados.

Cada caso de uso possui uma especificação declarativa em YAML. Essa
especificação registra a pergunta científica, o conjunto consultado, os
parâmetros aceitos e a operação executada. O mesmo arquivo define os resumos,
a visualização, as colunas da tabela e os limites de interpretação
apresentados na interface.

A estrutura compartilhada separa a definição analítica da implementação
visual. A API recebe o identificador do caso de uso e os parâmetros
selecionados, executa a operação correspondente no SQLite e devolve uma
resposta comum. Essa resposta contém o escopo analisado, os filtros ativos, as
medidas resumidas, a visualização e a tabela paginada.

A Figura~\ref{fig:c2-guided-structure} representa essa organização. As receitas
SQL permanecem associadas aos mesmos casos de uso, mas funcionam como
descrições reproduzíveis das consultas, e não como controles da interface.

\begin{figure}[htbp]
\centering
\fbox{
    \parbox{0.92\textwidth}{
        \centering

        \textbf{Especificação YAML do caso de uso}\\
        pergunta, conjunto, parâmetros, métrica, saída e interpretação

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{API de análises guiadas}\\
        resolução dos parâmetros e execução da consulta

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{Banco SQLite}

        \vspace{0.6em}
        $\downarrow$

        \vspace{0.4em}
        \textbf{Resposta compartilhada}\\
        escopo, sumários, visualização e tabela paginada

        \vspace{0.8em}

        \textbf{Receita SQL associada}\\
        tabelas, junções, filtros, agregações e ordenação
    }
}
\caption{Organização dos casos de uso do módulo de análises guiadas. A
especificação YAML define a pergunta e a apresentação dos resultados, enquanto
a API executa a consulta correspondente no banco SQLite. A receita SQL
documenta uma forma de reproduzir a operação sobre a mesma versão do banco.}
\label{fig:c2-guided-structure}
\end{figure}

Os casos de uso são distribuídos em três grupos. UC1--UC4 usam o composto como
unidade principal. UC5 e UC6 agregam as relações por via. UC7 e UC8 partem de
símbolos gênicos e acrescentam o contexto dos KOs associados. A
Tabela~\ref{tab:c2-guided-use-cases} apresenta a pergunta, a operação e o
limite de interpretação de cada análise.

\begin{table}[htbp]
\centering
\caption{Casos de uso disponíveis no módulo de análises guiadas.}
\label{tab:c2-guided-use-cases}
\scriptsize
\begin{tabularx}{\textwidth}{
    @{}
    p{0.65cm}
    p{1.65cm}
    p{3.45cm}
    p{4.35cm}
    X
    @{}
}
\toprule
\textbf{ID}
& \textbf{Entrada}
& \textbf{Pergunta analítica}
& \textbf{Métrica e resultado}
& \textbf{Limite de interpretação} \\
\midrule

UC1
& Composto
& Quais compostos apresentam maior amplitude de anotação funcional por KO?
& Ordenação pela contagem de KOs distintos, com gráfico de barras e tabela.
& A contagem descreve cobertura de anotação e não confirma degradação ou
eficiência de remediação. \\

UC2
& Composto e endpoint
& Quais compostos apresentam os maiores valores toxicológicos preditos no
contexto selecionado?
& Ordenação pelo valor de um endpoint ou pelo máximo dentro de um grupo, com
mapa de calor e tabela.
& Os valores são predições dependentes do endpoint e não constituem medidas
experimentais ou classificação de perigo. \\

UC3
& Composto
& Quais compostos combinam maior amplitude de anotação gênica e maior valor
toxicológico predito?
& Gráfico de dispersão entre \texttt{gene\_count} e a medida toxicológica,
com limiares do percentil 75 no conjunto filtrado.
& Os quadrantes são relativos ao recorte analisado; \texttt{gene\_count} é
uma aproximação da amplitude de anotação, não do desempenho biológico. \\

UC4
& Composto e referência
& Quais compostos estão presentes nas fontes institucionais selecionadas?
& Filtragem por código de referência, ordenação pela contagem de registros
institucionais e distribuição por fonte.
& A presença indica inclusão na fonte integrada e não substitui a avaliação
do enquadramento jurídico vigente ou da magnitude do risco. \\

UC5
& Via
& Quais vias concentram o maior número de KOs distintos?
& Contagem de KOs por combinação entre via e fonte, com ranking e tabela.
& A contagem não mede completude da via, expressão, atividade enzimática ou
fluxo metabólico. \\

UC6
& Via e endpoint
& Quais vias estão associadas a compostos acima do limiar toxicológico
selecionado?
& Contagem e proporção de compostos acima do limiar, com gráfico de barras,
mapa de calor e tabela.
& A associação depende do endpoint e do limiar selecionado e não demonstra
um mecanismo toxicológico causado pela via. \\

UC7
& Símbolo gênico e KO
& Quais genes estão relacionados ao maior número de compostos distintos?
& Contagem de compostos por símbolo gênico, enriquecida pelo número de KOs,
com ranking e tabela.
& A conectividade no banco não mede expressão, atividade catalítica ou
importância metabólica. \\

UC8
& Gene e endpoint
& Quais genes estão associados a compostos acima do limiar toxicológico
selecionado?
& Contagem de compostos acima do limiar e distribuição dos valores por gene,
com barras, boxplot e tabela.
& A relação é associativa e depende de predições do endpoint; não estabelece
causalidade entre o gene e a toxicidade. \\

\bottomrule
\end{tabularx}
\end{table}

As operações de ordenação usam critérios definidos para cada caso de uso. As
contagens são calculadas sobre entidades distintas para evitar que a repetição
de linhas da representação integrada aumente artificialmente os resultados.
Os casos baseados em toxicidade também excluem valores ausentes e registram o
endpoint ou o grupo de endpoints usado na consulta.

As visualizações complementam as tabelas sem introduzir métricas adicionais.
Os rankings usam gráficos de barras; as relações entre amplitude de anotação e
toxicidade predita usam gráfico de dispersão; e as análises dependentes de
endpoints usam mapas de calor ou boxplots. A interpretação permanece vinculada
ao conjunto filtrado e aos parâmetros registrados para a execução.

Cada caso de uso possui uma receita SQL correspondente. As receitas identificam
as tabelas consultadas, as junções, as condições de filtragem, as agregações e
a ordenação aplicadas. Um pesquisador pode repetir a consulta sobre o banco
SQLite da mesma versão usando os mesmos parâmetros, sem depender da
representação visual fornecida pela aplicação.

Os arquivos YAML e as receitas SQL são versionados junto ao código do
Database Explorer. Uma alteração na pergunta, na métrica, nos filtros ou no
critério de ordenação permanece associada à versão do software que contém a
nova definição. Essa organização permite distinguir mudanças de apresentação
de alterações no procedimento analítico.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]
{figures/chapter_04/fig_04_guided_analysis.png}
\iffalse
\fbox{
    \parbox{0.90\textwidth}{
        \centering
        Inserir captura do módulo \textit{Guided Analysis}.\\[0.5em]
        A imagem deve mostrar a seleção de um caso de uso, os filtros ativos,
        os cartões de resumo, a visualização e a tabela de resultados.\\[0.5em]
        Recomenda-se usar o UC3 para representar o gráfico de dispersão ou o
        UC6 para representar uma análise com ranking e mapa de calor.
        \vspace{3em}
    }
}
% Substituir o placeholder por:
% \includegraphics[width=\textwidth]
% {figures/chapter_04/fig_04_guided_analysis.png}
\fi
\caption{Módulo de análises guiadas do BioRemPP Database Explorer. Cada caso
de uso reúne uma pergunta analítica, parâmetros de consulta, medidas
resumidas, visualização e tabela de resultados sob uma estrutura comum.}
\label{fig:c2-guided-analysis}
\end{figure}

% -----------------------------------------------------------------------------
\section{Resultados do componente}
\label{sec:c2-resultados}
% -----------------------------------------------------------------------------

\escrever{Reunir aqui: (i) screenshots dos cinco módulos em operação (uma figura consolidada ou uma por módulo); (ii) exemplo de execução de pelo menos dois recipes de Guided Analysis (UC3 e UC7 são bons candidatos porque exercitam eixos analíticos complementares); (iii) tabela de métricas operacionais (contagens de cliques ou sessões, se coletadas), ou nota indicando que a plataforma não coleta métricas de uso por design de privacidade.}

\todo{Confirmar se há coleta de métricas de uso e, em caso positivo, incluir. Em caso negativo, mencionar explicitamente que a decisão foi arquitetural (privacy by design), o que reforça o argumento de FAIR compliance.}

% -----------------------------------------------------------------------------
\section{Discussão específica do componente}
\label{sec:c2-discussao}
% -----------------------------------------------------------------------------

O \gls{dbx} concretiza, do lado da interface, a mesma decisão editorial de compound-centricidade que estrutura o banco (Capítulo~\ref{cap:comp1}). Os cinco módulos oferecem cinco pontos de entrada para o mesmo grafo integrado, mas todos convergem para o composto como locus onde as evidências se reúnem.

A separação explícita entre \gls{dbx} e \gls{bws} (Capítulo~\ref{cap:comp3}) responde a uma preocupação editorial recorrente em recursos web de bioinformática: um único artefato que tenta fazer navegação de banco e análise de dados de usuário tipicamente acaba fazendo mal ambos. O \gls{dbx} é read-only por decisão; o \gls{bws} é upload-only por decisão. Cada um tem contrato limpo e implementação otimizada para seu contrato.

O módulo Guided Analysis também tem valor pedagógico além do valor analítico: expõe padrões de consulta que a comunidade de pesquisa considera úteis, servindo como documentação executável de como o banco pode ser interrogado. Um pesquisador que nunca escreveu SQL consegue extrair valor científico real das UC1--UC8; um pesquisador que domina SQL usa os recipes como referência para consultas mais elaboradas contra o CSV baixado.

Comparado a interfaces equivalentes de recursos correlatos --- KEGG browser, ChEBI browser, EnviPath UI, PlasticDB browser --- o \gls{dbx} não busca profundidade em nenhuma dimensão individual. Sua contribuição é a linkagem consistente das quatro dimensões (função, via, toxicidade e prioridade regulatória) sob um modelo de navegação flexível por entidade.

